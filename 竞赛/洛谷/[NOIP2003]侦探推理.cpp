#include <bits/stdc++.h>

using namespace std;

int m,n,p;
string name[30];
string speak[130];

int main(void){
	ios::sync_with_stdio(0);
	cin.tie(nullptr);
	cin >> m >> n >> p;
	for(int i = 0; i < m; i++)
		cin >> name[i];
	for(int i = 0; i < p; i++)
		getline(cin,speak[i]);
	judge()
	
	for(int g = 1; g <= n; g++)
	for(int d = 1; d <= 7; d++)
	{
		for(int )	
		
	}

	
	
	
	
	return 0;	
}
