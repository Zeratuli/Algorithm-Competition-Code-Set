#include <bits/stdc++.h>

using namespace std;

int main(void){
	long long int n,t;
	string s,r;
	
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	cin >> n >> t;
	cin >> s;
	
	if(t > n)
		cout << s << endl;
	else{
		int f = 0;
		
		for(int i = 0; i < n; i++)
		{
			if(s[i] == '.')
				x = i;	
				break;
		}
		for(int i = 0; i < n; i++)
		{
			if(s[n-i-1] >= '5'&&s[n-i-1]<= '9')
			{
					
				
			}
			
		}
	
	return 0;
		
}
